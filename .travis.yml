language: python

python:
  - "3.6"

install:
  - pip install -r requirements.txt
  - pip install -e .
  - pip install --user codecov

script:
  - pytest --cov=ecs_task_run --cov-report=xml

after_success:
  - codecov
